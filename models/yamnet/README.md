# YAMNet model directory

The YAMNet `.tflite` model file is fetched by `make fetch-yamnet` and is **not committed** to the repo.

See [`docs/audio-scene-classifier.md`](../../docs/audio-scene-classifier.md) for setup, performance notes, and the curated class whitelist.
